# Hero Scroll Animation Tasks

- [x] 1. **Video Cinematic Exit (app.js)**: Implement GSAP ScrollTrigger to scale down, blur, and fade out the video container as the user scrolls past the hero section.
- [x] 2. **Scroll Reveal (app.js)**: Add GSAP ScrollTrigger logic for `[data-reveal]` elements to fade-up smoothly as they enter the viewport.
- [x] 3. **Hardware Acceleration (styles.css)**: Ensure `.video-container` has `will-change` properties for 60fps animation.
